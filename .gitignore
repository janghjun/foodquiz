# 의존성
node_modules/

# 빌드 결과물 (Vercel이 직접 빌드하므로 커밋 불필요)
dist/

# 환경변수
.env
.env.local
.env.*.local

# 에디터 / OS
.DS_Store
*.local
.vscode/settings.json

# Claude Code 로컬 설정 (개인 설정, 저장소에 포함하지 않음)
.claude/
